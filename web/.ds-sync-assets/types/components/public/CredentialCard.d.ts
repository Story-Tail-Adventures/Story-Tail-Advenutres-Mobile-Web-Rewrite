/** One credential row (design: C2011 / M2011 credentials). Values come from the claims registry. */
export declare function CredentialCard({ title, detail }: {
    title: string;
    detail?: string;
}): import("react").JSX.Element;
