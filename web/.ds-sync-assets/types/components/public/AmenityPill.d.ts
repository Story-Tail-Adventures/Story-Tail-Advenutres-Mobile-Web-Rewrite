import { type IconName } from "../ui/Icon";
/** Icon + short label on a tonal surface (design: 2.0.5 amenity grid). */
export declare function AmenityPill({ icon, label }: {
    icon: IconName;
    label: string;
}): import("react").JSX.Element;
