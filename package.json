{
  "name": "storytail-adventures",
  "version": "0.0.1",
  "private": true,
  "description": "Story-Tail Adventures CRM platform \u2014 mobile + web + Supabase backend",
  "license": "UNLICENSED",
  "workspaces": [
    "web",
    "contracts"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "generate:contracts": "npm run generate -w contracts",
    "supabase:start": "supabase start",
    "supabase:stop": "supabase stop",
    "supabase:reset": "supabase db reset && ./supabase/scripts/seed-storage-objects.sh",
    "supabase:types": "mkdir -p web/types && supabase gen types typescript --local > web/types/supabase.ts && supabase gen types typescript --local > supabase/functions/_shared/database.types.ts",
    "supabase:seed-storage": "./supabase/scripts/seed-storage-objects.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
