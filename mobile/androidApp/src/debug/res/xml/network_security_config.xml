<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG ONLY. Lives in src/debug/, so it is never merged into a release build.

  Android 9+ blocks cleartext HTTP by default, and local Supabase serves plain HTTP on
  port 54321. Rather than setting android:usesCleartextTraffic="true" on the main
  manifest — which would ship — this permits cleartext to the two loopback hosts only:

    10.0.2.2  the Android emulator's alias for the host machine
    localhost for a device tunnelled with `adb reverse`
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
